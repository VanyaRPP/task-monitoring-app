import { Invoice } from '../..'

const Placing: React.FC<{
  record: Invoice
  edit?: boolean
  preview?: boolean
}> = ({ record, edit, preview }) => {
  return <>Розміщення</>
}

export default Placing
