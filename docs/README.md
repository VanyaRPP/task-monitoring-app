# Documentation

📚 **The engineering handbook lives in Notion (canonical):**
https://pleasant-foxtail-f25.notion.site/9cabed3d76fd4f7c8b02fcd3ac679fef → **Engineering Handbook**

It covers onboarding, architecture, the roles & permissions model, the data
model, feature modules, user flows, API reference, deployment, and contributing.

## Other docs in this repo

- **Live API reference** — run the app and open [`/docs`](http://localhost:3000/docs)
  (Swagger UI, generated from `@swagger` JSDoc via `next-swagger-doc`).
- **AI tooling rules** — see [`.claudedocs/`](../.claudedocs/): code style,
  the authoritative tech-stack list, and project rules.

## Assets

Screenshots referenced by the Notion handbook live here and are embedded into
Notion via raw GitHub URLs:

- `docs/screenshots/` — feature-module page & modal captures
- `docs/user-flow/image/` — user-flow captures

> Keep narrative docs in Notion (single source of truth) — don't reintroduce
> scattered `*.md` files in the repo.
