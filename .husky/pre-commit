#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Fast gate: type-check + run only tests affected by staged changes.
# Full coverage gate (with thresholds) runs on pre-push — see .husky/pre-push.
yarn types:check
yarn test:changed
